{
  "framework": null,
  "buildCommand": "sh -c \"mkdir -p .vercel/output/static && if [ -d public ]; then cp -R public/* .vercel/output/static; else cp -R ./* .vercel/output/static; fi\"",
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1" }
  ]
}
